# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime state / secrets
*.db
*.log
config.json          # generated per-node (node_id/psk/endpoints) — never commit
.env
*.local

# NOTE: dist/eef-node-dist.zip IS committed (nodes pull it from GitHub),
# so dist/ is NOT ignored here.
